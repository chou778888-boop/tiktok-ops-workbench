# 项目最高设计原则

以下三条适用于整个 TikTok 经营中枢，高于单个页面、单次需求和临时话术：

1. 所有设计必须经过全局思考。不要被用户当下的话术直接带着走；先结合整个工作台的信息架构、业务闭环、数据关系和长期影响做客观分析，再选择方案。
2. 所有设计必须以团队真实使用方便、减少操作步骤和提高效率为前提。排版必须精美、整齐、舒适；不得出现孤字、缺字或不合理换行；按钮文案保持完整；表格及关键内容在上下、左右方向正确对齐和居中。
3. 所有设计和代码必须整齐、先进、清晰且方便后续修改。项目会在边想边做的过程中持续调整，因此模块边界、命名、纯函数、测试和文档都必须支持持续优化，避免一次性堆叠和难以维护的实现。
4. 每次设计或调整时，如果用户意图、业务口径、使用场景或全局影响仍不明确，必须主动提出必要的问题，并逐步确认到真正理解意图后再定稿。不要为了显得推进快而带着关键误解实施；也不要询问能够从现有工作台、数据和上下文中安全判断的琐碎问题。

## 执行要求

- 修改页面前先检查相邻模块、导航入口、数据来源和团队实际操作路径；不要只优化局部截图。
- 需要澄清时一次聚焦一个关键问题，说明该问题会影响的设计判断；获得答案后继续推进，直到不存在会改变方案的关键歧义。
- 优先保留已有功能和真实数据，构建、重构和部署不得覆盖团队业务数据。
- 桌面端和约 320px 窄屏都必须检查；页面不得出现整体横向溢出。
- 表格数字按语义对齐，表头与内容保持清晰对应；需要横向滚动时只允许表格或矩阵自身滚动。
- 新业务规则放入对应业务模块，复杂计算优先写成可测试的纯函数；不要把业务逻辑堆入初始化或全局事件文件。
- 修改源码后先运行聚焦测试，再运行完整检查和本地浏览器回归；不要直接编辑 `dist/`。
